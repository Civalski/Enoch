/** Cabeçalho e atalhos do painel: ver `app/app/layout.tsx` (acesso comum a todas as rotas `/app`, incl. institucional). */
export default function AppBackofficeLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
